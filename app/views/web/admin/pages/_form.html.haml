= javascript_include_tag 'web/pages'

.row-fluid
  .col-md-10
    = simple_form_for @page, url: { controller: 'web/admin/pages', action: action }, html: { class: 'form-horizontal' } do |f|
      = f.input :title, as: :string
      = f.input :body
      = f.input :slug, as: :string
      = f.button :submit
      = link_to t('helpers.back'), admin_pages_path, class: "btn"
